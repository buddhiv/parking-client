export interface ParkingState {
    parkingSlots: any,
    userParking: any
}

export interface IParkingResponse {
    config?: any,
    data?: any,
    headers?: any,
    request?: any,
    status?: number,
    statusText?: string,
    token: string
}